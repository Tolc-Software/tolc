// This file contains includes to standard library headers which should work

#include <set>
#include <string>
#include <unordered_map>
#include <vector>

void f(std::set<int>);
void f(std::string);
void f(std::unordered_map<int, int>);
void f(std::vector<int>);
